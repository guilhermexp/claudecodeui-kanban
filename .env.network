# Network Access Configuration
# Este arquivo configura a aplicação para acesso na rede local

# Server Configuration
HOST=0.0.0.0
PORT=7347

# Frontend Configuration  
VITE_HOST=0.0.0.0
VITE_PORT=5892

# API URLs - Serão substituídos pelo IP real ao iniciar
VITE_API_URL=http://localhost:7347
VITE_WS_URL=ws://localhost:7347

# Security Settings
JWT_SECRET=your-secret-key-change-this
SESSION_SECRET=your-session-secret-change-this

# Vibe Kanban Backend
VIBE_BACKEND_PORT=6734

# Optional: Limit connections to specific IPs (comma-separated)
# ALLOWED_IPS=192.168.1.100,192.168.1.101

# Optional: Enable HTTPS (requires certificates)
# HTTPS_ENABLED=false
# SSL_CERT_PATH=./certs/cert.pem
# SSL_KEY_PATH=./certs/key.pem